const MOVIE_API='https://fabrizio-romanio-backend.onrender.com';
const $=s=>document.querySelector(s);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const img=v=>v||'';
async function api(path){try{const r=await fetch(MOVIE_API+path,{headers:{Accept:'application/json'}});if(!r.ok)throw new Error('API '+r.status);return await r.json()}catch(e){console.warn(e);return null}}
function initNav(){const b=$('.menu-btn'),n=$('.nav-links');if(!b||!n)return;b.addEventListener('click',()=>n.classList.toggle('mobile-open'));n.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>n.classList.remove('mobile-open')))}
function card(v){const id=v.id,title=v.title||v.name||'Untitled',year=v.year||'',poster=img(v.poster_url||v.image_url||v.poster||v.thumbnail_url);return `<article class="movie-card" data-id="${esc(id)}"><div class="movie-poster">${poster?`<img src="${esc(poster)}" alt="${esc(title)}" loading="lazy">`:'<span style="color:#fff;display:grid;place-items:center;height:100%;font-weight:900">NO POSTER</span>'}<span class="movie-type">${esc(v.type||'movie')}</span></div><div class="movie-card-body"><h3>${esc(title)}</h3><div class="movie-meta">${esc(year||'Release year unavailable')}</div>${v.tmdb_id?`<span class="movie-score">TMDB #${esc(v.tmdb_id)}</span>`:''}</div></article>`}
function bind(){document.querySelectorAll('.movie-card').forEach(c=>{c.onclick=()=>location.href='movie-detail.html?id='+encodeURIComponent(c.dataset.id)})}
async function loadMovies(){const grid=$('#movieGrid'),q=$('#movieSearchInput')?.value.trim()||'';grid.innerHTML='<div class="movie-empty">Loading movie catalogue…</div>';let data;if(q){data=await api('/api/movies?q='+encodeURIComponent(q)+'&type=movie')}else{const sort=$('.movie-tab.active')?.dataset.sort||'popularity_desc';data=await api('/api/movies?type=movie&sort='+encodeURIComponent(sort)+'&limit=12&region=US')}const items=q?(data?.title_results||[]):(data?.titles||[]);if(!items.length){grid.innerHTML='<div class="movie-empty"><strong>No movies found.</strong><br>Try another title or search term.</div>';return}grid.innerHTML=items.map(card).join('');bind()}
function setup(){initNav();document.querySelectorAll('.movie-tab').forEach(t=>t.addEventListener('click',()=>{document.querySelectorAll('.movie-tab').forEach(x=>x.classList.remove('active'));t.classList.add('active');loadMovies()}));$('#movieSearchForm')?.addEventListener('submit',e=>{e.preventDefault();loadMovies()});loadMovies()}
function trailerId(value){const s=String(value||'');return s.match(/[?&]v=([^&]+)/)?.[1]||s.match(/youtu\.be\/([^?&]+)/)?.[1]||s.split('/').pop()||''}
async function loadDetail(){initNav();const id=new URLSearchParams(location.search).get('id');const root=$('#movieDetail');if(!id){root.innerHTML='<div class="movie-empty">No movie selected.</div>';return}const data=await api('/api/movies/'+encodeURIComponent(id)+'?append=cast-crew');if(!data||data.error){root.innerHTML='<div class="movie-empty">Movie details are unavailable right now.</div>';return}const d=data, title=d.title||d.name||'Movie';document.title=title+' — Movie Centre';root.innerHTML=`<div class="movie-detail-layout"><div class="movie-detail-poster">${d.posterLarge||d.poster||d.poster_url?`<img src="${esc(d.posterLarge||d.poster||d.poster_url)}" alt="${esc(title)}">`:''}</div><div class="movie-detail"><span class="eyebrow">MOVIE CENTRE</span><h1>${esc(title)}</h1><div class="meta">${esc(d.release_date||d.year||'')} ${d.runtime_minutes?'· '+esc(d.runtime_minutes)+' min':''} ${d.imdb_rating?'· IMDb '+esc(d.imdb_rating):''} ${d.user_rating?'· User '+esc(d.user_rating):''}</div><p class="overview">${esc(d.overview||'No overview is available for this title yet.')}</p><div class="movie-actions">${d.trailer?`<button class="movie-action" id="watchTrailer">▶ Watch trailer</button>`:''}<a class="movie-action secondary" href="movies.html">← Back to movies</a></div></div></div><div class="availability"><h2>Where to watch</h2><div id="sourceList" class="source-list"><div class="movie-empty">Checking streaming availability…</div></div><p class="watchmode-note">Streaming availability is supplied by Watchmode. Availability and links can change by country and provider.</p></div>${d.cast?.length?`<div class="availability"><h2>Cast</h2><div class="cast-grid">${d.cast.slice(0,10).map(c=>`<div class="cast-card">${c.headshot_url?`<img src="${esc(c.headshot_url)}" alt="${esc(c.name||'Cast member')}">`:''}<div><strong>${esc(c.name||'Unknown')}</strong><br>${esc(c.character||c.role||'')}</div></div>`).join('')}</div></div>`:''}${d.trailer?`<div class="availability"><h2>Trailer</h2><div class="trailer-box"><iframe src="https://www.youtube-nocookie.com/embed/${encodeURIComponent(trailerId(d.trailer))}" title="Movie trailer" allow="accelerometer; autoplay; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div>`:''}`;const sources=await api('/api/movies/'+encodeURIComponent(id)+'/sources?region=US');const list=Array.isArray(sources?.sources)?sources.sources:[];$('#sourceList').innerHTML=list.length?list.map(s=>`<div class="source-card"><strong>${esc(s.name||'Streaming provider')}</strong><small>${esc(s.type||'available')} · ${esc(s.region||'US')}</small>${s.web_url?`<a href="${esc(s.web_url)}" target="_blank" rel="noopener noreferrer">Open provider →</a>`:''}</div>`).join(''):'<div class="movie-empty">No streaming source was returned for this region.</div>';$('#watchTrailer')?.addEventListener('click',()=>document.querySelector('.trailer-box')?.scrollIntoView({behavior:'smooth'}))}
document.addEventListener('DOMContentLoaded',()=>{if($('#movieGrid'))setup();if($('#movieDetail'))loadDetail()});
